# =========================================================================
#  Hordebreakers — Unity .gitattributes
#  Requires Git LFS:  run `git lfs install` once, and commit THIS file before
#  importing large assets so LFS catches them from the first commit.
#  (Project: keep Asset Serialization = Force Text — the Unity default — so the
#   YAML merge rules below work.)
# =========================================================================

# Normalize line endings; Git decides text vs binary unless overridden below.
* text=auto

# ---- Source / text ----
*.cs              text diff=csharp
*.shader          text
*.cginc           text
*.hlsl            text
*.compute         text
*.json            text
*.md              text
*.txt             text
*.xml             text
*.uxml            text
*.uss             text

# ---- Unity YAML — use Smart Merge (UnityYAMLMerge) and force LF ----
#  (To enable the driver: configure `merge.unityyamlmerge` in your git config
#   per Unity's "Smart merge" docs. Optional but saves pain on scene/prefab merges.)
*.unity           merge=unityyamlmerge eol=lf
*.prefab          merge=unityyamlmerge eol=lf
*.mat             merge=unityyamlmerge eol=lf
*.anim            merge=unityyamlmerge eol=lf
*.controller      merge=unityyamlmerge eol=lf
*.asset           merge=unityyamlmerge eol=lf
*.meta            merge=unityyamlmerge eol=lf
*.physicMaterial  merge=unityyamlmerge eol=lf
*.physicsMaterial2D merge=unityyamlmerge eol=lf

# =========================================================================
#  Git LFS — large binary assets (Synty models, textures, audio, etc.)
# =========================================================================

# ---- 3D models ----
*.fbx    filter=lfs diff=lfs merge=lfs -text
*.FBX    filter=lfs diff=lfs merge=lfs -text
*.obj    filter=lfs diff=lfs merge=lfs -text
*.blend  filter=lfs diff=lfs merge=lfs -text
*.dae    filter=lfs diff=lfs merge=lfs -text
*.3ds    filter=lfs diff=lfs merge=lfs -text
*.ma     filter=lfs diff=lfs merge=lfs -text
*.mb     filter=lfs diff=lfs merge=lfs -text
*.c4d    filter=lfs diff=lfs merge=lfs -text
*.max    filter=lfs diff=lfs merge=lfs -text

# ---- Textures / images ----
*.png    filter=lfs diff=lfs merge=lfs -text
*.PNG    filter=lfs diff=lfs merge=lfs -text
*.jpg    filter=lfs diff=lfs merge=lfs -text
*.jpeg   filter=lfs diff=lfs merge=lfs -text
*.tga    filter=lfs diff=lfs merge=lfs -text
*.tif    filter=lfs diff=lfs merge=lfs -text
*.tiff   filter=lfs diff=lfs merge=lfs -text
*.psd    filter=lfs diff=lfs merge=lfs -text
*.exr    filter=lfs diff=lfs merge=lfs -text
*.hdr    filter=lfs diff=lfs merge=lfs -text
*.bmp    filter=lfs diff=lfs merge=lfs -text
*.gif    filter=lfs diff=lfs merge=lfs -text

# ---- Audio ----
*.wav    filter=lfs diff=lfs merge=lfs -text
*.mp3    filter=lfs diff=lfs merge=lfs -text
*.ogg    filter=lfs diff=lfs merge=lfs -text
*.aif    filter=lfs diff=lfs merge=lfs -text
*.aiff   filter=lfs diff=lfs merge=lfs -text
*.flac   filter=lfs diff=lfs merge=lfs -text

# ---- Video ----
*.mp4    filter=lfs diff=lfs merge=lfs -text
*.mov    filter=lfs diff=lfs merge=lfs -text
*.webm   filter=lfs diff=lfs merge=lfs -text

# ---- Fonts ----
*.ttf    filter=lfs diff=lfs merge=lfs -text
*.otf    filter=lfs diff=lfs merge=lfs -text

# ---- Other heavy binaries ----
*.cubemap filter=lfs diff=lfs merge=lfs -text
*.unitypackage filter=lfs diff=lfs merge=lfs -text
